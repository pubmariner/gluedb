%n1:id
  %n1:id= id
  - if obj.class == Person
    %n1:is_authoritative= obj.is_authoritative?
    %n1:alias_ids
      - obj.members.each do |member|
        %n1:alias_id
          %n1:id= member.hbx_member_id
