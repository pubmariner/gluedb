%n1:id
  %n1:id= id
- if obj.class == "Person"
  %n1:is_authoratative= obj.is_authoratative?
  %n1:alias_ids
  - person.members.each do |member|
    %n1:alias_id
      = render 'shared/v2/identity', id: api_v2_people_url(hbx_id:member.hbx_member_id), obj: member