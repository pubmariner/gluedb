<% if transactions.count < 1 %>
  <div class="row"><div class="col-md-offset-1 col-md-2"><p>None found</p></div></div>
<% else %>
  <table class="table ">
    <thead>
      <tr>
        <th>Type</th>
        <th>State</th>
        <th>Submitted at</th>
        <th>Location</th>
        <% if can? :read, current_user %>
          <th>Action</th>
        <% end %>
      </tr>
    </thead>
    <tbody>
      <% transactions.each do |trans| %>
      <tr>
        <td><span class="fui-export">&nbsp;</span> <%= trans.transaction_kind.humanize %></td>
        <td class='capitalize-text'><%= trans_aasm_state(trans) %></td>
        <td><%= trans.submitted_at.to_s %></td>
        <td><%= trans.location %></td>
        <td>
          <% if can? :read, current_user %>
            <%= link_to 'View Detail', legacy_cv_transaction_path(trans) %>
          <% end %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
<% end %>